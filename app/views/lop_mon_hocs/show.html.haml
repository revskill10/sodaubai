%p#notice= notice

%p
  %b Ma lop:
  = link_to @lop_mon_hoc.ma_lop, @lop_mon_hoc

%h2 Lich Hoc:
- @lichs.each do |lich|
  = link_to lich.strftime("%Y-%m-%d"), lop_mon_hoc_buoihoc_path(@lop_mon_hoc, lich.strftime("%Y-%m-%d"))
  %br

- @lop_mon_hoc.sinh_viens.each do |sv|
  = link_to sv.username, sv if sv.user
  = sv.ten unless sv.user
  %br/
%br/

\|
= link_to 'Back', root_path
