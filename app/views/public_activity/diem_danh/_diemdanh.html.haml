- if activity.trackable
  - lich = activity.trackable.lich_trinh_giang_day
  %li
    = activity.owner.username    
    đã đánh vắng #{activity.recipient.hovaten || ""} vào buổi học #{link_to TkbGiangVien::THU2[lich.wday] + ", ngày " + lich.ngay_day.localtime.strftime("%d/%m") + ", " + lich.ngay_day.localtime.strftime("%Hh:%M") , lop_mon_hoc_buoihoc_path(lich.lop_mon_hoc_id, lich.ngay_day.localtime.strftime("%Y-%m-%d-%H-%M"))} vào
    = lich.updated_at.localtime.strftime("%Hh:%M %d/%m/%y")